# Userese 领域上下文

## 项目定位

Userese 帮助宿主 Agent 让产品“说用户听得懂的话”。它负责界面内容发现、知识核实、内容诊断、范围确认和 Writer 编排；Writer 只负责在已确认 brief 内写候选文案。

## 核心对象

| 对象 | 含义 | 不可混淆之处 |
|---|---|---|
| Surface | 一个用户可以体验的界面边界，由路由、角色、语言、设备和运行状态共同定义 | 不等于单个源码文件 |
| Surface map | 深入分析前建立的低成本页面、状态和内容来源地图 | 不是最终文案清单，也不证明完整覆盖 |
| Content source | 界面文字的来源，例如 HTML、组件、SSR、API、CMS、国际化资源或运行时模板 | 来源位置不决定它是否面向用户 |
| Content candidate | 通过机械提取得到、尚未经过详细语义判断的文字候选 | 不等于需要改写的文案 |
| Inventory item | 已确认属于覆盖边界并带有位置、用途、可见性和范围状态的展示内容 | 不等于 Writer 输入 |
| Coverage | 已检查哪些页面、状态和来源，以及哪些仍不可访问 | 不等于用户批准的改写范围 |
| Core expression | 承担页面主要理解、判断或行动任务的标题、正文、案例、说明和 CTA | 不等于“只存在于静态 HTML 的文字” |
| Complete surface | 目标界面的全部可发现内容，包括状态、图注、元信息、无障碍和动态模板 | 不等于整个项目 |
| Project-wide audit | 跨页面、渠道和内容来源检查一致性与冲突 | 不等于无差别读取每个仓库文件 |
| Authored copy | 产品团队可控制、用于向用户表达的文字 | 与实时业务数据、用户生成内容分开 |
| Business data | 订单、价格、数量、名称等运行时事实 | 默认不交给 Writer 自由改写 |
| User-generated content | 评论、昵称、正文等由最终用户产生的内容 | 只能审阅承载它的提示和标签 |
| Knowledge evidence | 用来证明受众、行为、主张和限制的项目或用户证据 | 现有界面文案不能证明自身正确 |
| Brief | 用户确认范围后交给 Writer 的 `userese-brief/v1` | 不包含结构建议和未解决的高风险事实 |
| Recommendation | 结构、产品、视觉或证据方面的独立建议 | 批准文案不自动批准建议 |

## 当前事实源

- 已发布行为：`SKILL.md`、`references/`、`scripts/`、`tests/`
- v0.3.0 需求：`spec/v0.3/REQUIREMENTS.md`
- v0.3.0 交互表达：`design/versions/v0.3/DESIGN_SPEC.md`
- 结构与长期决定：`docs/adr/`
- 过程材料：`.working/`，不是正式事实源

## 长期边界

- Userese 以用户体验到的界面为发现对象，以来源追踪保证可修改性和事实边界。
- 机械读取文件或响应不等于把全部内容发送给模型；模型上下文应只接收完成任务所需的候选、证据和局部上下文。
- 无法访问的登录态、CMS、后台、远端接口或个性化状态必须成为覆盖限制。
- Writer、去 AI 味步骤、工作区应用和生产发布都需要各自明确的用户决定。
