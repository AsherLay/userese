interface:
  display_name: "Userese"
  short_description: "把项目里面向用户的语言找出来，按你指定的读者改一遍"
  default_prompt: "使用 $userese 把这个项目里面向用户的语言找出来。先确认我希望谁来看，再按这个读者视角给出整体改写提案；先不要修改源文件。"
policy:
  allow_implicit_invocation: true
